[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cntp"
version = "0.1.0"
description = "A library for generating and processing point clouds from the timelapse photogrammetry"

[tool.setuptools.packages.find]
where = ["."]